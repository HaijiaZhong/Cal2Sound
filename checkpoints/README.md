# Checkpoints

No Cal2Sound checkpoints or external pretrained models are included.
Availability, compatible configurations, decoder setup, and loading instructions
are TBD. Nothing is downloaded automatically.

Local `.pth`, `.pt`, `.ckpt`, and `.safetensors` files are ignored by Git.
External pretrained models remain subject to their original licenses;
this repository does not claim their copyright.

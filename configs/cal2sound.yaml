# Experimental parameters are not yet confirmed.
model:
  name: Cal2Sound

data:
  input_dim: null
  input_frames: null
  output_dim: null
  output_frames: null

training:
  batch_size: null
  learning_rate: null
  epochs: null

evaluation:
  pitch_tolerance_cents: null

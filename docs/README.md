# Project Page

`index.html` is a minimal placeholder for a future GitHub Pages project page.
Open it locally to preview. No site has been deployed; the repository URL is TBD.

Future assets can be organized in `audios/`, `figures/`, and `css/`.
Only approved examples and confirmed paper details should be added.
